---
title: "Reproducible documents"
author: "Aino R. Peltonen"
format: html
---
- Aino R. Peltonen, MSc.
- PhD Fellow, Anne Loft's groups, FGM, BMB, SDU
- Affiliation NNFC at Broad Institute

# Intro
In this thesis **we** will study *how* adipose tissue and liver communicate in obesity.

# Methods and Results
There's something light about *wet lab* but the main emphasis will be in **computational tools**.

## Analysis
We analyzed **so** much.

# Discussion
And then we have some *deep* remarks of biology and how our findings take science **forward**.

```{r setup}
#| message: false
#| warning: false
#| echo: false
library(tidyverse)
library(NHANES)
nhanes_small <- read_csv(here::here("data/NHANES_small.csv"))
```

```{r}
nhanes_small <- select(nhanes_small, age, sex, bmi, diabetes, phys_active, bp_sys_ave, bp_dia_ave, education)
nhanes_small
```


```{r}
2 + 2
```
# Table of results
```{r}
nhanes_small %>%
  filter(!is.na(diabetes)) %>%
  group_by(diabetes, education) %>%
  summarise(
    mean_age = round(mean(age, na.rm = TRUE), 2),
    mean_bmi = mean(bmi, na.rm = TRUE) %>%
      round(3)
  ) %>%
  ungroup() %>%
  knitr::kable(caption = "Mean values of age and BMI for education and diabetes status")
```

## Prettier table
```{r}
#| echo: false
#| warning: false
nhanes_small %>%
  filter(!is.na(diabetes), !is.na(education)) %>%
  group_by(diabetes, education) %>%
  summarise(
    mean_age = mean(age, na.rm = TRUE),
    mean_bmi = mean(bmi, na.rm = TRUE)
  ) %>%
  ungroup() %>%
  mutate(
    # Task 2a.
    mean_age = round(mean_age, 1),
    mean_bmi = round(mean_bmi, 1),
    # Task 2b.
    education = str_to_sentence(education)
  ) %>%
  rename(
    # Task 3.
    "Diabetes status" = diabetes,
    "Education" = education,
    "Mean Age" = mean_age,
    "Mean BMI" = mean_bmi
  ) %>%
  knitr::kable(caption = "Mean values of Age and BMI for each education and diabetes status.")
```

# Adding figures

Cute cow in @fig-cow

![Image by Kim Hansen from Wikimedia Commons.](Figures_files/Images/Cow.jpg){#fig-cow width="50%" height="50%"}

# Plotting

### One variable plot

```{r}
#| fig-cap: "Distribution of BMI"
#| label: fig-bmi-histo
#| message: false

nhanes_small %>%
  filter(bmi <= 60) %>%
  ggplot(aes(x = bmi)) +
  geom_histogram(na.rm = TRUE)
```
```{r}
#| label: fig-diabetes-education
#| fig-cap: "Count of Diabetes and Education"
#| fig-subcap:
#| - "Number of those with different levels of education"
#| - "Number of those with or without diabetes"
#| layout-ncol: 2

nhanes_small %>%
  filter(!is.na(education)) %>%
  ggplot(aes(x = education)) +
  geom_bar()

nhanes_small %>%
  filter(!is.na(diabetes)) %>%
  ggplot(aes(x = diabetes)) +
  geom_bar()
```
### Plotting two variables
```{r}
nhanes_small %>%
  ggplot(aes(x = bmi, y = bp_sys_ave)) +
  geom_hex(na.rm = TRUE) +
  geom_smooth(na.rm = TRUE)
```
### Plotting two discrete variables
```{r}
nhanes_small %>%
  filter(!is.na(education), !is.na(diabetes)) %>% # could also be &, but in this function , works like &
  ggplot(aes(x = diabetes, fill = education)) +
  geom_bar(position = position_dodge())
```
## Plotting discrete and continous variables
```{r}
nhanes_small %>%
  filter(!is.na(diabetes), bmi <= 60) %>%
  ggplot(aes(x = diabetes, y = bmi)) +
  geom_violin(na.rm = TRUE)
```
## Exercise to make plots with one or two variables

```{r}
library(NHANES)
nhanes_exercise <- NHANES %>%
  rename_with(snakecase::to_snake_case) %>%
  rename(sex = gender)
```

```{r}
#| label: fig-distribution-age-diabetes_age
#| layout-ncol: 2
#| fig-cap: "Patient age at collection and age at diagnosis"
#| fig-subcap: 
#|   - "Density of patient age at collection"
#|   - "Density of patient age when diagnosed with diabetes"
# Distribution of age
nhanes_exercise %>% 
  ggplot(aes(x = age)) +
    geom_density(na.rm = TRUE)

# Distribution of age at diabetes diagnosis
nhanes_exercise %>% 
  ggplot(aes(x = diabetes_age)) +
    geom_density(na.rm = TRUE)
```

```{r}
# Number of people who smoke now and are or above 20 years of age,
# removing those with missing smoking status.
nhanes_exercise %>% 
  filter(age >= 20, !is.na(smoke_now)) %>% 
  ggplot(aes(x = smoke_now)) +
  geom_bar()
```

```{r}
nhanes_exercise %>% 
    mutate(mean_arterial_pressure = (bp_sys_ave + (2 * bp_dia_ave)) / 3) %>% 
    ggplot(aes(x = bmi, y = mean_arterial_pressure)) +
    geom_hex(na.rm = TRUE) +
    geom_smooth()
```

